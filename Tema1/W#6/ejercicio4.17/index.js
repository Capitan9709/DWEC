// WS#6 --> 4.17

for (let num=1;num<=10;num++) {

    document.write("<h2>Tabla del " + num + "</h2>");

    for (let t=1;t<=10;t++) {
        document.write(num + "x" + t + "=" + (num*t) + "<br>");
    }

}
